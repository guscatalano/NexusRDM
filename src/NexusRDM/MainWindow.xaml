<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="NexusRDM.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:NexusRDM.Views"
    Title="Nexus RDM">

    <Grid>
        <NavigationView x:Name="NavView"
                        PaneDisplayMode="LeftCompact"
                        IsBackButtonVisible="Collapsed"
                        OpenPaneLength="260"
                        CompactPaneLength="48"
                        SelectionChanged="NavView_SelectionChanged">

            <NavigationView.MenuItems>
                <NavigationViewItem Tag="connections" Content="Connections" IsSelected="True">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="World" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Tag="audit" Content="Audit Log">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Clock" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem Tag="settings" Content="Settings">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Setting" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>

            <Grid ColumnDefinitions="280,1,*">
                <views:ConnectionsPane x:Name="ConnectionsPane" Grid.Column="0" />

                <Border Grid.Column="1"
                        Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

                <TabView x:Name="SessionTabs"
                         Grid.Column="2"
                         TabWidthMode="SizeToContent"
                         IsAddTabButtonVisible="False"
                         TabCloseRequested="SessionTabs_TabCloseRequested" />
            </Grid>

        </NavigationView>
    </Grid>
</Window>
