<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="NexusRDM.Views.RdpSessionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowDefinitions="Auto,*,Auto">

        <!-- Toolbar -->
        <CommandBar Grid.Row="0" DefaultLabelPosition="Collapsed" Background="Transparent">
            <AppBarTextBlock Text="{x:Bind ViewModel.Host, Mode=OneTime}"
                             Style="{ThemeResource CaptionTextBlockStyle}"
                             VerticalAlignment="Center" Margin="8,0" />
            <AppBarSeparator />
            <AppBarButton Label="Ctrl+Alt+Del" ToolTipService.ToolTip="Send Ctrl+Alt+Del"
                          Click="SendCtrlAltDel_Click">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE756;" />
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton Icon="DisconnectDrive" Label="Disconnect"
                          Command="{x:Bind ViewModel.DisconnectCommand}" />
        </CommandBar>

        <!--
            RdpHostPanel is the HWND anchor. We retrieve its HWND via
            WindowNative + interop and pass it to RdpSession.Connect().
            The mstsc child window is reparented into this panel's HWND.
        -->
        <Border x:Name="RdpHostPanel" Grid.Row="1" Background="#000000"
                SizeChanged="RdpHostPanel_SizeChanged" />

        <!-- Status bar -->
        <Border Grid.Row="2" BorderThickness="0,1,0,0"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" Padding="8,4">
            <Grid ColumnDefinitions="*,Auto">
                <TextBlock Grid.Column="0"
                           Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                           Style="{ThemeResource CaptionTextBlockStyle}"
                           VerticalAlignment="Center" />
                <ProgressRing Grid.Column="1"
                              IsActive="{x:Bind ViewModel.IsConnecting, Mode=OneWay}"
                              Width="16" Height="16" />
            </Grid>
        </Border>

    </Grid>
</UserControl>
