<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="NexusRDM.Views.SshSessionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:NexusRDM.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowDefinitions="Auto,*,Auto">

        <!-- Session toolbar -->
        <CommandBar Grid.Row="0" DefaultLabelPosition="Collapsed" Background="Transparent">
            <AppBarTextBlock Text="{x:Bind ViewModel.Host, Mode=OneTime}"
                             Style="{ThemeResource CaptionTextBlockStyle}"
                             VerticalAlignment="Center" Margin="8,0" />
            <AppBarSeparator />
            <AppBarButton Icon="DisconnectDrive" Label="Disconnect"
                          Command="{x:Bind ViewModel.DisconnectCommand}" />
        </CommandBar>

        <!-- Terminal surface — custom TerminalControl renders VT output here -->
        <controls:TerminalControl x:Name="Terminal" Grid.Row="1"
                                  FontFamily="Cascadia Code, Consolas, Courier New"
                                  FontSize="14" />

        <!-- Status bar -->
        <Border Grid.Row="2" BorderThickness="0,1,0,0"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                Padding="8,4">
            <Grid ColumnDefinitions="*,Auto">
                <TextBlock Grid.Column="0"
                           Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                           Style="{ThemeResource CaptionTextBlockStyle}"
                           VerticalAlignment="Center" />
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12">
                    <TextBlock x:Name="SizeLabel" Style="{ThemeResource CaptionTextBlockStyle}"
                               VerticalAlignment="Center" />
                    <ProgressRing IsActive="{x:Bind ViewModel.IsConnecting, Mode=OneWay}"
                                  Width="16" Height="16" />
                </StackPanel>
            </Grid>
        </Border>

    </Grid>
</UserControl>
